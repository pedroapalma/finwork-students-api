spring:
  cloud:
    application-name: studentsapi
    vault:
      application-name: studentsapi
      host: ${VAULT_HOST:localhost}
      port: 8201
      scheme: http
      authentication: TOKEN
      token: ${VAULT_TOKEN:00000000-0000-0000-0000-000000000000}